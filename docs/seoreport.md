จากการวิเคราะห์โครงสร้างโปรเจกต์ของคุณผ่านไฟล์ `package.json` และ `README.md` ร่วมกับตัวอย่างระบบ CRM ที่คุณให้มา นี่คือเอกสารแนะนำโมดูล **Accounting (ระบบบัญชี)** ที่ออกแบบให้สอดคล้องกับเทคโนโลยีที่คุณใช้จริง (Next.js 15, Prisma, Tailwind CSS, MUI v7)

---

# SEO Report - Accounting Module

โมดูล **Accounting** เป็นส่วนสำคัญของระบบ SEO Report ที่ใช้จัดการข้อมูลทางการเงิน รายได้จากการบริการ และการออกรายงานภาษีเบื้องต้น พัฒนาด้วยเทคโนโลยี Modern Stack (Next.js 15 App Router) เพื่อความรวดเร็วและความปลอดภัยของข้อมูล

---

## 🚀 Features

| Feature                  | Description                                                       |
| ------------------------ | ----------------------------------------------------------------- |
| **Revenue Dashboard**    | แสดงภาพรวมรายได้ รายเดือน/ปี พร้อมกราฟเปรียบเทียบด้วย Recharts    |
| **Invoice Management**   | จัดการใบแจ้งหนี้/ใบเสร็จรับเงิน สำหรับลูกค้าที่ใช้บริการ SEO      |
| **Expense Tracking**     | บันทึกค่าใช้จ่ายต้นทุนบริการ (เช่น ค่า API, ค่า Content, ค่า Ads) |
| **Tax Summary**          | สรุปยอดภาษีหัก ณ ที่จ่าย (3%) และ VAT เพื่อส่งต่อให้ฝ่ายบัญชี     |
| **Payment Verification** | ระบบตรวจสอบการชำระเงินและอัปโหลดหลักฐานการโอนเงิน                 |
| **Auto-Reporting**       | ออกรายงานสรุปกำไร-ขาดทุนรายโปรเจกต์ (Project-based Profitability) |

---

## 🛠️ Tech Stack

**Frontend & Framework:**

- **Next.js 15 (App Router)** - SSR และ API Routes
- **React 19** - UI Management
- **Material-UI (MUI) v7** - UI Components & Data Grid
- **Tailwind CSS v4** - Layout & Styling
- **Recharts** - การแสดงผลกราฟสถิติการเงิน

**Backend & Database:**

- **Prisma ORM** - จัดการ Schema และ Database Connection
- **NextAuth.js v4** - จัดการสิทธิ์การเข้าถึงเมนูบัญชี
- **Zod** - ตรวจสอบความถูกต้องของข้อมูลการเงิน (Validation)

---

## 📁 Module Structure

```
src/app/(portal)/accounting/
├── page.tsx                    # หน้า Dashboard ภาพรวมบัญชี
├── invoices/                   # จัดการใบแจ้งหนี้
│   ├── page.tsx                # รายการ Invoice ทั้งหมด
│   └── [id]/page.tsx           # รายละเอียดและการแก้ไข Invoice
├── expenses/                   # จัดการค่าใช้จ่าย
│   └── page.tsx
└── reports/                    # หน้าออกรายงานการเงิน
    └── page.tsx

src/components/accounting/
├── Dashboard/                  # ส่วนประกอบ Dashboard (Charts, Cards)
│   ├── RevenueChart.tsx
│   └── SummaryStats.tsx
├── Invoices/                   # ส่วนประกอบที่เกี่ยวกับ Invoice
│   ├── InvoiceTable.tsx
│   ├── InvoiceStatusBadge.tsx
│   └── PaymentDialog.tsx
└── Forms/                      # ฟอร์มบันทึกข้อมูล
    ├── ExpenseForm.tsx
    └── InvoiceCreateForm.tsx

src/hooks/accounting/           # Custom Hooks สำหรับ Logic บัญชี
├── useInvoices.ts              # จัดการ CRUD ใบแจ้งหนี้
├── useExpenses.ts              # จัดการข้อมูลค่าใช้จ่าย
└── useAccountingStats.ts       # คำนวณค่าทางสถิติการเงิน

```

---

## 🔄 Accounting Workflow

```
[Service Rendered] → [Create Invoice] → [Wait for Payment] → [Verify Payment] → [Close Project]
        ↓                  ↓                    ↓                   ↓                ↓
   เก็บข้อมูล SEO     ส่งเมล์ให้ลูกค้า       ตรวจสอบสลิป        ออกใบเสร็จ       สรุปกำไรราย Project

```

---

## 👥 Role-Based Access (Accounting)

การเข้าถึงข้อมูลการเงินจะถูกควบคุมผ่าน **RoleGuard** และ Middleware

| Role           | Permissions                                          |
| -------------- | ---------------------------------------------------- |
| **Admin**      | ดูข้อมูลการเงินได้ทั้งหมด, ลบ/แก้ไขรายการย้อนหลัง    |
| **Accounting** | จัดการ Invoice, บันทึกค่าใช้จ่าย, ออกรายงานภาษี      |
| **SEO Dev**    | ดูได้เฉพาะต้นทุน/รายได้ของ Project ที่ตนเองรับผิดชอบ |
| **Customer**   | ดูและดาวน์โหลดได้เฉพาะใบแจ้งหนี้ของตนเอง             |

---

## 📦 Key Implementation Examples

### Invoice Validation (Zod)

ใช้ตรวจสอบข้อมูลก่อนบันทึกลงฐานข้อมูลเพื่อให้มั่นใจว่าตัวเลขถูกต้อง:

```typescript
const invoiceSchema = z.object({
  customerId: z.string(),
  amount: z.number().min(0),
  taxRate: z.number().default(7),
  status: z.enum(["PENDING", "PAID", "CANCELLED"]),
});
```

### Database Schema (Prisma)

โครงสร้างข้อมูลเบื้องต้นใน `schema.prisma`:

```prisma
model Invoice {
  id        String   @id @default(cuid())
  amount    Float
  status    String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  customer  User     @relation(fields: [customerId], references: [id])
  customerId String
}

```

---

## 🚀 Getting Started

```bash
# ติดตั้ง Library ที่เกี่ยวข้อง (ถ้ายังไม่มี)
npm install recharts @mui/x-data-grid

# Run ระบบพัฒนา
npm run dev

```

---

**Current Version:** 0.1.0
**Last Updated:** December 2025
