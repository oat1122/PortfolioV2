---
# Complaint Hub - Core Functionality Module

ระบบ **Complaint Hub** พัฒนาขึ้นเพื่อเป็นศูนย์กลางในการรับเรื่องร้องเรียน การจัดการสถานะแบบเรียลไทม์ และการวิเคราะห์ข้อมูลสถิติ โดยใช้โครงสร้าง **Next.js 15 App Router** และจัดการฐานข้อมูลด้วย **Prisma ORM**
---

## 🚀 Features

| Feature                       | Description                                                              |
| ----------------------------- | ------------------------------------------------------------------------ |
| **Complaint Submission**      | ฟอร์มแจ้งเรื่องร้องเรียนพร้อมระบบตรวจสอบข้อมูลด้วย Zod และการอัปโหลดไฟล์ |
| **Real-time Notifications**   | ระบบแจ้งเตือนเจ้าหน้าที่เมื่อมีเคสใหม่ผ่าน Server-Sent Events (SSE)      |
| **Case Tracking**             | ระบบติดตามสถานะสำหรับผู้แจ้งเหตุโดยใช้รหัส Tracking Number               |
| **Statistical Dashboard**     | การแสดงผลข้อมูลเชิงสถิติและแนวโน้มการร้องเรียนด้วยกราฟ Recharts          |
| **Role-Based Access Control** | ระบบจัดการสิทธิ์การเข้าถึงข้อมูลตามบทบาท (Admin, Manager, Staff)         |
| **Data Export Service**       | ฟังก์ชันส่งออกข้อมูลข้อร้องเรียนทั้งหมดเป็นไฟล์ภายนอกเพื่อการทำรายงาน    |

---

## 🛠️ Tech Stack

**Frontend:**

- **Next.js 15 (App Router)** - Framework หลักของระบบ
- **Shadcn UI + Tailwind CSS** - การออกแบบ UI และ Layout
- **Recharts** - การแสดงผลข้อมูลสถิติในหน้า Dashboard
- **React Hook Form + Zod** - การจัดการและตรวจสอบความถูกต้องของฟอร์ม

**Backend:**

- **Next.js API Routes** - การสร้าง RESTful API สำหรับจัดการข้อมูล
- **Prisma ORM** - ตัวกลางจัดการฐานข้อมูล PostgreSQL
- **NextAuth.js** - ระบบพิสูจน์ตัวตนและความปลอดภัย
- **Server-Sent Events (SSE)** - การส่งข้อมูลแบบ Real-time

---

## 📁 Module Structure

```
src/
├── app/api/                        # ส่วนจัดการ Backend API
│   ├── complaint/                  # API สำหรับรับเรื่องและจัดการเคส
│   ├── notifications/sse/          # API สำหรับระบบ Real-time SSE
│   └── statistics/                 # API สำหรับดึงข้อมูลสถิติกราฟ
├── components/                     # ส่วนประกอบของ UI แยกตามโมดูล
│   ├── complaint/                  # ฟอร์มรับเรื่องและการติดตามสถานะ
│   ├── complaints/                 # ตารางรายการและการคัดกรองเคส
│   └── dashboard/                  # ส่วนประกอบ Dashboard และ Charts
├── lib/                            # Library และ Utility สำคัญ
│   ├── sse/connection-manager.ts   # ตัวจัดการการเชื่อมต่อ Real-time
│   ├── validation/schemas.ts       # กำหนดโครงสร้างตรวจสอบข้อมูล (Zod)
│   └── db/prisma.ts                # การเชื่อมต่อฐานข้อมูล Prisma
└── middleware.ts                   # ระบบความปลอดภัยและการตรวจสอบสิทธิ์

```

---

## 🔄 Workflow การจัดการข้อร้องเรียน

```
[User Submits] → [Auto-Notification] → [Officer Updates] → [User Tracks Status]
       ↓                 ↓                    ↓                    ↓
  ส่งเรื่อง+ไฟล์      แจ้งเตือนเจ้าหน้าที่      ปรับสถานะ/ใส่หมายเหตุ     ตรวจสอบผ่านรหัส Tracking

```

---

## 🔧 Key Implementation Logic

### Real-time Update Logic (SSE)

ใช้จัดการการส่งข้อมูลแจ้งเตือนไปยังเจ้าหน้าที่โดยไม่ต้อง Refresh หน้าจอ:

```typescript
// อ้างอิงจาก src/lib/sse/connection-manager.ts
export class ConnectionManager {
  private connections: Map<string, ReadableStreamDefaultController> = new Map();
  // ฟังก์ชันส่งข้อมูลไปยัง Client ที่เชื่อมต่ออยู่
  broadcast(data: any) {
    this.connections.forEach((controller) => {
      controller.enqueue(`data: ${JSON.stringify(data)}\n\n`);
    });
  }
}
```

### Data Validation (Zod Schema)

ตัวอย่างการตรวจสอบข้อมูลหัวข้อเรื่องร้องเรียน:

```typescript
// อ้างอิงจาก src/lib/validation/schemas.ts
export const complaintSchema = z.object({
  title: z.string().min(5, "หัวข้อต้องมีอย่างน้อย 5 ตัวอักษร"),
  category: z.string(),
  description: z.string().min(10),
});
```

---

## 📦 Database Schema (Prisma)

โครงสร้างหลักของตารางข้อมูลในระบบ `prisma/schema.prisma`:

```prisma
model Complaint {
  id          String   @id @default(uuid())
  trackingNo  String   @unique
  title       String
  status      String   @default("PENDING")
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  // ความสัมพันธ์กับตารางอื่นๆ
}

```

---
